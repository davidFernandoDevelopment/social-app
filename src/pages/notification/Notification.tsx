import './notification.scss';
import { PopupNotification } from '../../ui';

const Notification = () => {
    return (
        <div className='notification'>
            <PopupNotification />
        </div>
    );
};

export default Notification;